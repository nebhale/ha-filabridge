name: FilaBridge
version: "1.3.0"
slug: filabridge
description: Bridge PrusaLink-compatible printers to Spoolman
url: "https://github.com/nebhale/ha-filabridge"

arch:
  - amd64
  - aarch64

image: "ghcr.io/nebhale/ha-filabridge"

init: false

# Home Assistant's defaults provide startup: application, boot: auto, and
# stage: stable. They are omitted because the App linter rejects explicit
# declarations of default values.

# Store FilaBridge's SQLite database in Supervisor-managed persistent storage.
environment:
  FILABRIDGE_DB_PATH: "/data"

# The startup wrapper reads the generated ingress URL from the Supervisor API.
hassio_api: true
ingress: true
ingress_port: 5000
panel_icon: mdi:printer-3d-nozzle
panel_title: FilaBridge

# Stop FilaBridge while Supervisor captures its SQLite database.
backup: cold
